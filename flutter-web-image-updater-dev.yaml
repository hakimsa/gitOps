apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: flutter-web-updater-dev
  namespace: argocd
spec:
  namespace: argocd   # <- aquí el namespace de las aplicaciones
  applicationRefs:
    - namePattern: flutter-web-dev  # <- coincidir exactamente con la app
      useAnnotations: false
      images:
        - alias: flutter-web-image-dev
          imageName: hakimsamouh/flutter-web
          allowTags: ^dev-.*
          manifestTargets:
            kustomize:
              name: hakimsamouh/flutter-web
  commonUpdateSettings:
    updateStrategy: newest-build
  writeBackConfig:
    method: argocd